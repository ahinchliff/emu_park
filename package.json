{
  "name": "lambda_terraform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "yarn build && cd api && yarn package && cd ../ && yarn terraform-apply && rm -rf build",
    "encrypt-config": "aws-vault exec gotcha-dev -- sh ./terraform/scripts/encrypt-terraform-secret.sh",
    "terraform-apply": "cd ./terraform/environments/development && aws-vault exec gotcha-dev -- terraform apply",
    "destroy": "cd ./terraform/environments/development && aws-vault exec gotcha-dev -- terraform destroy && rm -rf .webpack",
    "build": "tsc --build && rm -f tsconfig.tsbuildinfo",
    "up-test-env": "docker-compose up -f docker-compose-test.yml"
  },
  "dependencies": {
    "handlebars": "^4.7.6",
    "html-loader": "^1.1.0",
    "ts-loader": "^7.0.2",
    "typescript": "^3.9.2",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  }
}
