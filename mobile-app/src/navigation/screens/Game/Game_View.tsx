import * as React from "react";
import { Text } from "react-native";

type Props = {};

const GameScreenView: React.FC<Props> = (_props) => {
  return <Text>Hello</Text>;
};

export default GameScreenView;
